# Prefer Codex remote MCP configuration when available:
# url = "https://api.swarm.services/mcp"

[mcp_servers.swarm]
command = "npx"
args = ["-y", "@blackscaletech/swarm-mcp@0.3.0"]

# Run this once before starting Codex:
# npx -y @blackscaletech/swarm-mcp@0.3.0 connect
